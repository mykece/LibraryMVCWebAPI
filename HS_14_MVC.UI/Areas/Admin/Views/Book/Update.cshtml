@model HS_14_MVC.UI.Areas.Admin.Models.BookVMs.AdminBookUpdateVM

@{
    ViewData["Title"] = "Update";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Update</h1>

<h4>AdminBookUpdateVM</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Update">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="DateOfPublish" class="control-label"></label>
                <input asp-for="DateOfPublish" class="form-control" />
                <span asp-validation-for="DateOfPublish" class="text-danger"></span>
            </div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="IsAvailable" /> @Html.DisplayNameFor(model => model.IsAvailable)
                </label>
            </div>
            <div class="form-group">
                <label class="form-control">Authors</label>
                <select asp-for="AuthorId" asp-items="@Model.Authors" class="form-control">
                    <option value="">--Seçiniz--</option>
                </select>
            </div>
            <div class="form-group">
                <label class="form-control">Categories</label>
                <select asp-for="CategoryId" asp-items="@Model.Categories" class="form-control">
                    <option value="">--Seçiniz--</option>
                </select>
            </div>
            <div class="form-group">
                <label class="form-control">Publishers</label>
                <select asp-for="PublisherId" asp-items="@Model.Publishers" class="form-control">
                    <option value="">--Seçiniz--</option>
                </select>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
